{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tarif</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tarif.id }}</td>
            </tr>
            <tr>
                <th>Service</th>
                <td>{{ tarif.service }}</td>
            </tr>
            <tr>
                <th>Prixunitaire</th>
                <td>{{ tarif.prixunitaire }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tarif_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tarif_edit', { 'id': tarif.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
